
spring:
  datasource:
    url: jdbc:mariadb://210.207.161.10:3306/board?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: board
    password: board123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true

devtools:
  restart:
    enabled: true
  livereload:
    enabled: true

logging:
  level:
    org.hibernate.type: trace
